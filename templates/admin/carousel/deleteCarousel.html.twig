<form method="post" action="{{ path('admin_carousel_delete', {'id': carousel.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer ce Carousel');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carousel.id) }}">
    <button class="btn">Supprimer</button>
</form>
